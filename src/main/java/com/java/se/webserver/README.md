The Java SE web servers provide support for HTTP and HTTPS.

#### Usage:
1. Run `WebServer.java`
2. Go to [http://localhost/example](http://localhost/example) or [https://localhost/example](https://localhost/example) in your browser

